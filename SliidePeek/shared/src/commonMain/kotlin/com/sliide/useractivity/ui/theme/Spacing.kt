package com.sliide.useractivity.ui.theme

import androidx.compose.ui.unit.dp

/** Signal spacing scale — 4px base grid. From `design/design_tokens_and_handoff.html` §03. */
object Space {
    val s1 = 4.dp   // icon ↔ label gaps, tight insets
    val s2 = 8.dp   // chip padding, list-row gaps
    val s3 = 12.dp  // card padding, row inset
    val s4 = 16.dp  // screen gutters, section gaps
    val s5 = 20.dp  // pane padding (iPad detail)
    val s6 = 24.dp  // two-column gutter (iPad)
}
